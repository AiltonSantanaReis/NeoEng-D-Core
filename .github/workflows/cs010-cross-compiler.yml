name: CS010 cross-compiler evidence

on:
  workflow_dispatch:
  push:
    branches:
      - codex/changeset-010
    paths:
      - ".github/workflows/cs010-cross-compiler.yml"
      - "CMakeLists.txt"
      - "vcpkg.json"
      - "include/**"
      - "src/**"
      - "modules/distributed_reference/**"
      - "scripts/qualification/run_distributed_reference_campaign.py"
      - "scripts/qualification/verify_distributed_reference_evidence.py"

permissions:
  contents: read

jobs:
  campaign:
    name: Linux x86_64 ${{ matrix.compiler }}
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - compiler: gcc
            cc: gcc
            cxx: g++
          - compiler: clang
            cc: clang
            cxx: clang++
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y libboost-dev ninja-build

      - name: Configure
        env:
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
        run: >-
          cmake -S . -B build/cs010-${{ matrix.compiler }} -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DBUILD_TESTING=ON
          -DNEOENG_DCORE_BUILD_FULL_TOOLSET=OFF
          -DNEOENG_DCORE_BUILD_RESEARCH_TOOLS=OFF
          -DNEOENG_DCORE_BUILD_VIEW_LAB=OFF

      - name: Build
        run: >-
          cmake --build build/cs010-${{ matrix.compiler }}
          --target neoeng_distributed_reference_tests
          neoeng_distributed_reference_probe

      - name: Run campaign
        run: >-
          python3 scripts/qualification/run_distributed_reference_campaign.py
          --tests build/cs010-${{ matrix.compiler }}/modules/distributed_reference/neoeng_distributed_reference_tests
          --probe build/cs010-${{ matrix.compiler }}/modules/distributed_reference/neoeng_distributed_reference_probe
          --output artifacts/cs010-linux-${{ matrix.compiler }}
          --compiler-id ${{ matrix.compiler }}

      - name: Independently verify evidence
        run: >-
          python3 scripts/qualification/verify_distributed_reference_evidence.py
          artifacts/cs010-linux-${{ matrix.compiler }} --write-report

      - uses: actions/upload-artifact@v4
        with:
          name: cs010-linux-${{ matrix.compiler }}
          path: artifacts/cs010-linux-${{ matrix.compiler }}
          if-no-files-found: error
          retention-days: 30

  compare:
    name: Compare GCC and Clang
    needs: campaign
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: cs010-linux-*
          path: artifacts

      - name: Compare semantic probe results
        run: |
          diff -u \
            artifacts/cs010-linux-gcc/raw-probe.json \
            artifacts/cs010-linux-clang/raw-probe.json \
            > cross-compiler.diff
          python3 - <<'PY'
          import hashlib
          import json
          from pathlib import Path

          gcc = Path("artifacts/cs010-linux-gcc/raw-probe.json")
          clang = Path("artifacts/cs010-linux-clang/raw-probe.json")
          digest = lambda p: hashlib.sha256(p.read_bytes()).hexdigest()
          result = {
              "schema": "neoeng.dcore.cs010-cross-compiler.v1",
              "status": "passed",
              "architecture": "x86_64",
              "compilers": ["gcc", "clang"],
              "gcc_probe_sha256": digest(gcc),
              "clang_probe_sha256": digest(clang),
              "byte_identical": gcc.read_bytes() == clang.read_bytes(),
              "cross_architecture_claim_promoted": False,
          }
          Path("cross-compiler-summary.json").write_text(
              json.dumps(result, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          if not result["byte_identical"]:
              raise SystemExit("GCC and Clang semantic results differ")
          PY

      - uses: actions/upload-artifact@v4
        with:
          name: cs010-cross-compiler-comparison
          path: |
            cross-compiler.diff
            cross-compiler-summary.json
          if-no-files-found: error
          retention-days: 30
